#include "stdafx.h"
#include "stdio.h"
#include "stdlib.h"
int pow(int a, int b)
{
int c=a;
for (int i = 1; i < b; i++)
c = c*a;

return c;
}

int main()
{
int a = 0;
int b = 0;
scanf("%d %d", &a, &b);
printf("%d\n", pow(a,b));
system("pause");
return 0;
}
